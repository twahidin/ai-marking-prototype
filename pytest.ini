[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers --tb=short
markers =
    requires_lualatex: skips when the `lualatex` binary isn't on PATH (UP-31 PDF smoke).
filterwarnings =
    ignore::DeprecationWarning
